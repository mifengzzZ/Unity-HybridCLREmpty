using UnityEngine;

public class GameObj : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("热更绑定在AB包资源上的脚本");
        Debug.Log("测试1");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
